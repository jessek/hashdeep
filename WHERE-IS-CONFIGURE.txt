The "configure" script is not checked into the SVN because it is locally generated.

To create the configure script, use this command:

   $ autoreconf

You will need to have the following packages installed:

   autoconf
   automake
   libtool or glibtool

